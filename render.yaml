services:
  - type: web
    name: puzzle-solver
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn puzzle_solver:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: FLASK_ENV
        value: production

